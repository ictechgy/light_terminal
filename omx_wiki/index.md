# lterm Project Wiki

Category: reference
Tags: wiki, index, command-surface

Repository-native notes for durable product and implementation context.

`omx_wiki/` is the repository-native wiki root used by OMX tooling for this
`lterm` project.

## Reference

- [[command-surface]] — canonical CLI vocabulary, compatibility aliases, and tmux shim boundary.
- [[tmux-compat-capture]] — `logs` versus `capture-pane` behavior, range semantics, and parser invariants.

## Conventions

- Wiki pages live under `omx_wiki/` and should be updated when command names, aliases, or compatibility semantics change.
- Keep user-facing product CLI names separate from `lterm tmux-compat` shim commands.
